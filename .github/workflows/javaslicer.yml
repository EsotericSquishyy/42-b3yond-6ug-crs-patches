name: Build and Push Java Slicer to GHCR

on:
  push:
    branches:
      - main
    paths:
      - 'components/javaslicer/**'
  release:
    types:
      - created

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log into Other‑Org GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ secrets.OTHER_ORG_GHCR_USERNAME }}
          password: ${{ secrets.OTHER_ORG_GHCR_TOKEN }}

      - name: Extract repository name
        run: echo "REPO_NAME=$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Build and push java slicing Docker image
        if: ${{ github.event_name != 'release' }}
        uses: docker/build-push-action@v5
        with:
          context: ./components/javaslicer
          file: ./components/javaslicer/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ env.REPO_NAME }}/crs-java-slicer:${{ github.sha }}
            ghcr.io/${{ env.REPO_NAME }}/crs-java-slicer:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
