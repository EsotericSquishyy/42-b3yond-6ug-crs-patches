name: Build and Push Prime Builder to GHCR

on:
  push:
    branches:
      - main
    paths:
      - 'components/prime-build/**'
  release:
    types:
      - created

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract repository name
        run: echo "REPO_NAME=$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Build and push prime-build Docker image
        if: ${{ github.event_name != 'release' }}
        uses: docker/build-push-action@v5
        with:
          context: ./components/prime-build
          file: ./components/prime-build/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ env.REPO_NAME }}/prime-build:${{ github.sha }}
            ghcr.io/${{ env.REPO_NAME }}/prime-build:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
